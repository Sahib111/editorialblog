var preloader = 'on';
			if(preloader=='on'||preloader=='ON'||preloader=='On'){
				$('body').addClass('preloader-on');                
			}
            else {
	          	$('body').addClass('preloader-off');
            }		
